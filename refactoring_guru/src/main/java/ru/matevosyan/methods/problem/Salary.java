package ru.matevosyan.methods.problem;

public class Salary {

    public double twoPercentRaise(double salary) {
        return salary + (salary * (2.0 / 100));
    }

    public double fivePercentRaise(int salary) {
        return salary + (salary * (5.0 / 100));
    }
}
